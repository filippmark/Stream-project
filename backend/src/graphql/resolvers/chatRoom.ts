const ChatRoom = require("../../models/ChatRoom");

module.exports = {
    chatRooms: async (args: any) => {
        const {userId} = args;
        
    },

    createChatRoom: async (args: any) => {
        
    }
}